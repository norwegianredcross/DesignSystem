import {
  SkipLink as DigDirSkipLink,
  type SkipLinkProps as DigDirSkipLinkProps,
} from '@digdir/designsystemet-react';

// Directly export the type from Digdir
export type SkipLinkProps = DigDirSkipLinkProps;

// Directly export the component from Digdir
export const SkipLink = DigDirSkipLink;

// Optional: If you still want a consistent display name
// SkipLink.displayName = 'SkipLink'; // Usually not needed
