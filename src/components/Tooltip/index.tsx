
import {
  Tooltip as DigDirTooltip,
  type TooltipProps as DigDirTooltipProps,
} from '@digdir/designsystemet-react';

// Directly export the type from Digdir
export type TooltipProps = DigDirTooltipProps;

// Directly export the component from Digdir
export const Tooltip = DigDirTooltip;

// Optional: If you still want a consistent display name
// Tooltip.displayName = 'Tooltip'; // Usually not needed
